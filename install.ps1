# install.ps1 — nexus-platform bundle operator entry point (Windows).
#
# Placeholder scaffold from NEX-282. See install.sh for the future shape
# per NEX-286 spec — both scripts converge on the same install flow.

$ErrorActionPreference = "Stop"

Write-Host @"
nexus-platform install.ps1 — scaffold placeholder.

Real install flow lands in NEX-286. See README.md + NEX-281 epic for
the full design.

If you're running this from a scaffold-era checkout, the bundle isn't
assembled yet. Check the project status at:

  https://carriedworlduniverse.atlassian.net/browse/NEX-281

"@

exit 0
